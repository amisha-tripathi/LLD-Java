package ObjectOrientedDesignPractice.ParkingLot;

public class ParkingAdmin extends Account {

  public ParkingAdmin(Person person, String accountId, String username, String password, String adminId) {
    super(person, accountId, username, password);
  }

  // CRUD operations of adding parking floor, parkingspots, parking displayboard, entracne agates, exits gates
}